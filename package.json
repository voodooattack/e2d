{
  "name": "e2d",
  "version": "1.0.3",
  "description": "A canvas rendering engine HEAVILY inspired by react.js to help enforce one way data flow patterns.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browserify-middleware": "^6.0.0",
    "express": "^4.13.1"
  },
  "browserify": {
    "transform": [
      "bulkify"
    ]
  },
  "dependencies": {
    "bulkify": "^1.1.1",
    "lodash": "^3.10.0",
    "square-matrix-multiply": "^0.2.2"
  },
  "keywords": ["canvas", "2d", "WebWorker", "graphics", "browser"]
}
